import React from "react";

// selecting default
// export default React.createContext("english");
const context = React.createContext("english");

console.log(context);

export default context;

// created provider and consumer react objects
